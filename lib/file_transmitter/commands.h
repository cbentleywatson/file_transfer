/*
 * This file is goig to hold formats for structs or whatever ascociated with  * commands. For instance a table of command numbers would go here.
 *
 */
#define DEFAULT_START_DELAY 500
// if a string is sent it's fine because it'll be converted to ascii by Serial.print
#define DEFAULT_START_ACK = 0;